import React, { Component } from 'react';

class componentName extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <h1>React with Express hot module replacement!</h1>
        );
    }
}

export default componentName;
